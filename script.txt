Private Sub Worksheet_Change(ByVal Target As Range)
    Dim cell As Range
    Dim watchRange As Range
    Set watchRange = Range("E3:E500") 
    
    ' Only run if the change happened in the slide number column
    If Not Intersect(Target, watchRange) Is Nothing Then
        ' Loop through changed cells in case you deleted/pasted multiple
        For Each cell In Intersect(Target, watchRange)
            
            ' If Column E has a number, make D blue/underlined
            If cell.Value <> "" And IsNumeric(cell.Value) Then
                With cell.Offset(0, -1).Font
                    .Color = RGB(0, 0, 255) ' Bright Blue
                    .Underline = xlUnderlineStyleSingle
                End With
            Else
                ' If Column E is blank/text, force D back to standard black
                With cell.Offset(0, -1).Font
                    .ColorIndex = xlAutomatic
                    .Underline = xlUnderlineStyleNone
                End With
            End If
            
        Next cell
    End If
End Sub

Sub RefreshLinkFormatting()
    Dim cell As Range
    ' This scans existing slide numbers and applies the look
    For Each cell In Range("E3:E500")
        If cell.Value <> "" And IsNumeric(cell.Value) Then
            With cell.Offset(0, -1).Font
                .Color = RGB(0, 0, 255)
                .Underline = xlUnderlineStyleSingle
            End With
        Else
            With cell.Offset(0, -1).Font
                .ColorIndex = xlAutomatic
                .Underline = xlUnderlineStyleNone
            End With
        End If
    Next cell
End Sub
