/* Keep the input in the DOM (so it posts), but block user interaction */
.completed-lock,
.completed-lock .ms-Toggle,
.completed-lock .ms-Checkbox { position: relative; }

.completed-lock::after {
  content: ""; position: absolute; inset: 0; z-index: 2;
  /* acts as a clear overlay that eats clicks/taps */
}

.completed-lock label { cursor: default !important; }
.completed-lock .ms-Toggle, .completed-lock .ms-Checkbox { opacity: .95; }

/* Optional: if allowed by your CSS area (not the per-field dropdown), this helps too */
.completed-lock input[type="checkbox"], .completed-lock .ms-Toggle-input { pointer-events: none; }
